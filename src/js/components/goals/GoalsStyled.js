import styled from 'styled-components';

import { COLORS, SPACINGS } from 'Styled/themes';

const GoalsStyled = styled.div`
  background: ${COLORS.WHITE};
  margin-bottom: ${SPACINGS.M};
  min-width: 70vh;
  padding: ${SPACINGS.M};
`;

export default GoalsStyled;
